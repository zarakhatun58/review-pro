import React from 'react';
import NavBar from '../NavBar/NavBar';
import './Header.css';

const Header = () => {
    return (
        <div className="nav-bg">
            <NavBar></NavBar>
        </div>
    );
};

export default Header;